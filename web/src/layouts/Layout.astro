---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0" />
    <title>{title} | Melisa Carpintero - Psicóloga Infantil</title>
    <meta name="description" content="Especialista en bienestar infantil y desarrollo emocional. Un espacio seguro para el crecimiento de los más pequeños en Panamá." />
    <link rel="icon" type="image/svg+xml" href="/Melisa-Portfolio/favicon.svg" />
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous">
    <!-- Load fonts asynchronously to avoid render blocking -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
    <noscript>
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&display=swap" rel="stylesheet">
    </noscript>
  </head>
  <body class="bg-brand-light text-brand-primary min-h-screen flex flex-col font-sans selection:bg-brand-accent selection:text-white">
    <Header />
    <main class="flex-grow flex flex-col w-full pt-20 overflow-x-hidden">
      <slot />
    </main>
    <Footer />
  </body>
</html>
